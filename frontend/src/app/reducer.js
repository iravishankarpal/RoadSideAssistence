const test = (state = [], action) => {
  switch (action.type) {
    case value:
      break;

    default:
      break;
  }
};

const test2 = (state = {}, action) => {
  switch (action.type) {
    case value:
      break;

    default:
      break;
  }
};

export default { test, test2 };
